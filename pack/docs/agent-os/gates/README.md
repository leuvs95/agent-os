# Gates

One file per unit: `docs/agent-os/gates/<unit-slug>.md`.

Write the probe **before** code. Pass criteria do not change to match the
implementation.

Template is in `templates/GATE.md` inside the agent-os pack; after import,
copy from this header.
