# Changelog

## 0.7.0 — 2026-08-20

Import copies `scripts/agent-os-check.sh`. Same `--force` keep-or-refresh
as other protocol files. README states the one stranger command:
`scripts/agent-os-check.sh --range`.

## 0.6.1 — 2026-08-20

Identity matches what import stamps. VERSION, CHANGELOG, and README name
`scripts/agent-os-check.sh` (the MMU). Import still does not copy the checker.

- VERSION is no longer 0.5.0 staffing-only

## 0.6.0 — 2026-08-19

Mechanical refuse. `pack/scripts/agent-os-check.sh` scores commit-by-commit
transitions against 30 frozen stories. Prompts document; the check enforces;
bypass is not PASS.

## 0.5.0 — 2026-08-19

Kernel: risk-driven + AI-native lenses. No hub library.

- TPM staffing at `/build-north-star` (≤5 seats, named substrates)
- Six lenses replace the nine ops categories; learned-only risk log
- Ladder ordered by risk retired
- Project-local `docs/agent-os/substrates/` generated at accept
- Import no longer copies catalogs; promote-to-hub removed from kernel
- TaxiLabs substrate files removed from the OS

## 0.4.0 — 2026-08-19

Seats and `/failure-catalog` are first principles. Catalogs evolve.

- Third seat is **Platform** (name substrates; do not assume a database)
- Failure skill: discover substrates → apply principle categories → overlay matching `catalogs/substrates/` files
- No matching file is expected (e.g. RAG). Write the project catalog; promote a new substrate file later
- Paid-for Postgres/webhook rows stay in the catalog, not in the seat question list

## 0.3.0 — 2026-08-19

`/build-north-star` is the locked design review that *creates* the law.

- Product, engineering, and database seats; each must object once
- Conversation lock while constitution is a stub — no product code, no stack-to-get-moving
- Understanding gate before files; `/grill-me` is a subroutine, not genesis
- `/north-star` remains a trigger alias in the skill description

## 0.2.0 — 2026-08-19

North Star is constitution + ladder, not one dated mega-file.

- `/north-star` distills planning chats into `CONSTITUTION.md` + `LADDER.md`
- `/amend-north-star` versions pivots with mandatory failure injection
- Status (shipped/deployed/proven) stays in WORKING_STATE and gates
- Off-ladder work and invariant conflicts cannot be “interpreted away”

## 0.1.0 — 2026-08-19

Seed. Reshapes the TaxiLabs learning OS around P0–P5:

- Unknown-unknowns as a mandatory catalog (not opt-in `/grill-me`)
- Independent gates written before code
- Reconstruction over line review
- Dual-AI catches become durable artifacts
- Substrate sprints before the first production write
- Ceremony cap: WORKING_STATE + gate evidence
