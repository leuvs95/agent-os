# Gate: 2

Written: 2026-08-20
Written before implementation: yes
Run by: owner

Unit 2 — identity. Kernel-local. Pass if / Fail if frozen before code.
Do not change this file to match the implementation.

**Contract:** VERSION, CHANGELOG, README name the MMU and match what import
ships. Out of scope: marketing; new features; copying the checker; hooks.

## Probe

From the repository root. Exit 0 only if every check holds.

```bash
set -euo pipefail
ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT"

V=$(tr -d '[:space:]' < VERSION)
[ -n "$V" ]
[ "$V" != "0.5.0" ]
[ "$V" != "1.0.0" ]
grep -qE "^## ${V}( |$)" CHANGELOG.md
grep -q 'scripts/agent-os-check.sh' README.md

# Latest CHANGELOG heading is this VERSION and that section names the MMU.
awk -v v="$V" '
  $0 ~ "^## " v "( |$)" {p=1; next}
  p && $0 ~ /^## / {exit}
  p {body=body $0 "\n"}
  END { if (body !~ /agent-os-check/) exit 1 }
' CHANGELOG.md

# Import still stamps VERSION; it does not copy the checker.
grep -q 'docs/agent-os/VERSION' scripts/import.sh
! grep -q 'pack/scripts/agent-os-check.sh' scripts/import.sh
```

## Pass if

- `VERSION` is a single non-empty line, not `0.5.0`.
- `CHANGELOG.md` has a heading `## <VERSION>` and that section names `agent-os-check`.
- `README.md` names `scripts/agent-os-check.sh`.
- `scripts/import.sh` still writes `docs/agent-os/VERSION` from `VERSION` and does not copy `pack/scripts/agent-os-check.sh`.

## Fail if

- `VERSION` is still `0.5.0` (staffing-only identity).
- CHANGELOG latest heading and `VERSION` disagree.
- README or the matching CHANGELOG section is silent on the checker.
- Import gained a checker copy (unit 3).
- Hooks, CI, or MMU `status_of()` / fixture EXPECT changes landed in this unit.
- Version is `1.0.0` because the constitution was accepted (v1 is rows 1–3, not this stamp).

## Catalog rows this protects

- VERSION 0.5.0 / CHANGELOG / README silent on the checker after 0.6.0 merged

## Result (fill when run)

- Ran at: 2026-08-20 (implementer ran probe; owner reconstructed 2026-08-20)
- Output: re-run after dropping Unit 1 Status grammar from the 0.6.1 section. Probe exit 0. `VERSION=0.6.1`. CHANGELOG `## 0.6.1` still names `agent-os-check`. README names `scripts/agent-os-check.sh`. Import still stamps `docs/agent-os/VERSION` and does not copy the checker.
- Decision: PASS — owner, 2026-08-20. Reconstruct pass. Pass if / Fail if unchanged. Not a merge.

## Follow-on (not this unit)

No `scripts/import.sh` checker copy. No hooks. No Status grammar reopen.
