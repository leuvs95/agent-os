# Gate: 8

Written: 2026-08-27
Written before implementation: yes
Run by: owner | second agent | implementer (discouraged)

Kernel-local. Not imported. Pass if / Fail if frozen before code.
Do not change this file to match the implementation. Do not edit
`docs/agent-os/gates/8-lot-clerk/` to make a new probe green.

**Contract:** Imported protocol produces a path, not a risk curriculum.
Lot Clerk gold + anti-fixtures prove both sides. SFTP mid-build inject
must not derail. Non-UI builder deltas (unknown expiry, lot identity)
must remain rows.

Out of scope: thaw 4–7; MMU header change; TaxiLabs CI; quality-of-work
scoring of live agents.

## Probe

From the agent-os repository root. Exit 0 only if every check holds.

```bash
docs/agent-os/gates/8_probe.sh
```

## Pass if

The probe exits 0, which requires all of:

**A. Lot Clerk gold** (`docs/agent-os/gates/8-lot-clerk/gold/`)

- Constitution has `## Observable Product Outcome` containing `Given`.
- Every catalog `###` risk has `Threatens-OPO:` yes|no and exactly one
  `Disposition:` of research-retired, gate-retired, builder-retired,
  use-retired, deferred.
- Ladder data-row count is strictly less than catalog risk count.
- Ladder ids are exactly `unknown` and `identity`.
- Those ids are builder-retired, threaten the OPO, and their
  `Product-delta:` names a required truth/capability (unknown/expiry
  preserved; stable lot identity) — not a screen.
- `sftp`, `scheduler`, `cache`, `ui`, `api` are not ladder ids.
- Amendment `AMENDMENTS/001-sftp-inject.md` exists, stamps SFTP
  `Disposition: deferred` / `Threatens-OPO: no`, and does not add a
  ladder row.

**B. Anti-fixtures** — `check_tree` must reject each of:

- `anti-every-risk-is-a-row`
- `anti-zero-builder`
- `anti-architecture-cleaner`
- `anti-plumbing-row`
- `anti-missing-opo`
- `anti-sftp-derail`
- `anti-gate-as-machinery`

**C. Imported protocol** (`pack/`)

- KERNEL law 5 names the five dispositions, observable product outcome,
  delete test, destination test, product delta, and the shorthand
  “Research broadly. Build narrowly.”
- The old one-line law 5 (`ordered by **risk retired**, sliced by
  contract, never by layer` as the whole law) is gone.
- Constitution stub has `## Observable Product Outcome`.
- Catalog stub has `Disposition:`.
- Ladder stub keeps the frozen five-column MMU header (no sixth
  column).
- `/build-north-star` assigns disposition before writing rows; the
  understanding gate recites OPO, not “scariest risk → row 1”.
- `/amend-north-star` asks whether new evidence threatens the OPO
  before adding a row.
- `/gate-first` allows a catalog probe with no unit.
- `/unit-unlock` refuses rows that are not builder-retired.
- Workflow next-unit is the highest-risk unresolved builder dependency
  on the shortest path to the OPO.
- No `pack/.cursor/skills/risk-disposition/` skill. No P6 in firing
  order. MMU `HEADER1` in `pack/scripts/agent-os-check.sh` unchanged.

## Fail if

The probe exits non-zero. In particular:

- Gold turns every catalog risk into a ladder row.
- Gold has zero builder rows.
- Gold missing-OPO, plumbing ids, or a UI-only product delta for
  `unknown` / `identity`.
- Any anti-fixture is accepted as a legal tree.
- SFTP inject becomes a ladder id.
- Protocol files still teach “ordered by risk retired” as sufficient
  ladder law, or add a skill/lens/P6/MMU column.

## Catalog rows this protects

- Following the kernel in good faith converts discovered risks into rows
- Disposition theater (everything deferred / zero builders)
- Gold green while KERNEL still mints a row per risk
- Gate-first requires a unit id
- Sixth ladder column breaks frozen MMU header

## Result (fill when run)

- Ran at: 2026-08-27
- Output: `docs/agent-os/gates/8_probe.sh: PASS` (gold OK; 7 antis rejected; protocol checks OK). `scripts/check-fixtures/run.sh`: 30 frozen fixtures matched.
- Decision: PASS
- Run by: implementer (owner asked this unit to run the gate in-session; Pass if was not edited)
